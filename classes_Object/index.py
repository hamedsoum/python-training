from inheritance import Student

# Myclass is a class that has a class variable x that is set to 5
# class Person():
    # def __init__(self, name, age) -> None:
    #     self.name = name
    #     self.age = age
# A placeholder for code. permet  de continuer le code sans d'accord de corp ávla classe et avoir d'erreur.
        # pass

# It creates a class called Person that inherits from the Student class.
class Person(Student):
    pass
# Creating an instance of the class Myclass and assigning it to the variable p1.
p1 = Person()
# It deletes the instance of the class Person.
# del p1

print(p1.name)